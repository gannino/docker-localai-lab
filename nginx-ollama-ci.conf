events {
    worker_connections 1024;
}

http {
    upstream docker_model_runner {
        server ollama:11434 max_fails=3 fail_timeout=30s;
        server 172.17.0.1:11434 max_fails=3 fail_timeout=30s backup;
    }

    server {
        listen 80;

        # Health check endpoint
        location /health {
            return 200 "OK";
            add_header Content-Type text/plain;
        }

        # Open WebUI compatibility - /ollama path
        location /ollama/ {
            proxy_pass http://docker_model_runner/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;

            # Handle streaming responses
            proxy_buffering off;
            proxy_cache off;
            proxy_read_timeout 300s;
            proxy_connect_timeout 75s;
        }

        # Direct API access
        location / {
            proxy_pass http://docker_model_runner;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;

            # Handle streaming responses
            proxy_buffering off;
            proxy_cache off;
            proxy_read_timeout 300s;
            proxy_connect_timeout 75s;
        }
    }
}
